<?php

$host = "localhost";
$user = "root";
$password = "";
$database = "oferta";

$conn = mysqli_connect($host, $user, $password, $database);

if($conn) {
    // echo "berhasil";
} else {
    // echo "gagal";
}
